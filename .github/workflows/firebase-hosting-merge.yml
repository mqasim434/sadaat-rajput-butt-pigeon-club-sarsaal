# This file was auto-generated by the Firebase CLI
# Modified for Flutter Web build support

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main   # or master, change if needed

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout repo
      - uses: actions/checkout@v4

      # ✅ Setup Flutter
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'   # put your Flutter version here

      # Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # Build Flutter Web
      - name: Build Flutter Web
        run: flutter build web --release

      # ✅ Deploy to Firebase Hosting
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PIGEON_TRACK }}
          channelId: live
          projectId: pigeon-track
